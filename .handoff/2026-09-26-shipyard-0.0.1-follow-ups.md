# Handoff: shipyard 0.0.1 follow-ups, from the configuration session

This session ran on the maintainer's Mac on 2026-09-26: Claude Code in Herdr tab 2 ("CC", marked settled at handover) of workspace `wJ`, session `a13dfd19-c8c6-4270-b053-0dc19e647560`. It worked in worktree `~/.treehouse/shipyard-1e47e3/1/shipyard` on branch `build/shipyard-core-0.0.x`. It set shipyard up for the maintainer's own use, pruned the shipyard skill, locked down agents' control of the Mac, and filed two tickets from the maintainer's first use. Everything it made is committed and pushed. The next orchestrator starts from the artifacts below, not from the conversation. The previous handoff, `.handoff/2026-09-26-shipyard-0.0.1-delivered.md`, still holds for everything this one doesn't change.

## Where things stand

- **Pull request [#21](https://github.com/yahyabedirhan/shipyard/pull/21)** is still open against `main` and waiting on the maintainer's review. Fixes land on the same branch; update its description through **to-pr** when they do. Its saved description is `.humanlayer/tasks/pr-21/description.md`.
- **Effort `shipyard-0-0-1`** (spec #1, `gh issue list --label effort:shipyard-0-0-1`). Open tickets:
  - **#44**, bug: the hover highlight jumps between rows instead of following the pointer. Reported by the maintainer; the ticket lists leads from the code (`matchedGeometryEffect` inside a `LazyVStack` with pinned headers, a spring animation on every row, enter/exit ordering). Not reproduced: agents can't drive the app (see below). It blocks #20.
  - **#45**, feature: ↑/↓ move the highlight through the rows, and Return opens the row. Blocked by #44, because both change the same highlight state.
  - **#42**, an intermittent reopen after the menu closes itself; seen once and not reproduced.
  - **#33**, tidy-ups deferred until 0.0.1 has merged.
  - **#20**, the maintainer's hand checks.
  - **#43**, the maintainer's configuration, is closed; its closing comment records the setup.
- **The maintainer's configuration:** `~/.config/shipyard/config.toml` has three projects (`shipyard`, `skills`, and `job search`, which is private), with `[defaults.issues] show = true` and every other setting at its default. The test projects are gone. At handover, another session (tab 3 of this workspace) was switching it to `[menu] layout = "tabs"` at the maintainer's request. The file is the maintainer's: edit it only when they ask, through the **shipyard** skill.
- **The shipyard skill:** pruned (`429d798`) and installed globally from this branch (`npx skills add "yahyabedirhan/shipyard#build/shipyard-core-0.0.x" -s shipyard -g -a codex -y`). Once #21 merges and the branch is deleted, reinstall it from `main`: `npx skills add yahyabedirhan/shipyard -s shipyard -g -a codex -y`.

## Decisions made in the conversation that aren't obvious from the artifacts

- **Agents don't drive the Mac.** The maintainer turned off Accessibility for iTerm. The Herdr server was started from iTerm, so iTerm's privacy grants cover every agent in Herdr; Ghostty is only the window. Clicks and System Events now fail ("osascript is not allowed assistive access"), and a CGEvent click is silently dropped. Screen Recording is still on, so screenshots work, but agents can't open the panel. This is on purpose. The rule is in `AGENTS.md` under Testing: verify with automated tests, don't retry or work around the block, and name any real-menu check for the maintainer in the handoff or the pull request. This replaces the previous handoff's "Checking the real app" section and #27's how-to. It also replaces the old per-ticket real-menu screenshot check: brief sub-agents on tests only.
- **Parallel worktrees still apply**, but the only step to serialise is the install. No agent does a real-menu pass.
- **#44 before #45**, in one line of work: they share the highlight state in `ListLayout.swift`, `TabsLayout.swift` and `RowButtonStyle` (`Components.swift`). #42 and #33 can run beside them. #33 waits for the merge, as before.
- **Make the highlight testable.** Both tickets ask for the rule that picks the highlighted row to live in code a test covers, such as a small highlight model in `ShipyardCore` or the app's view model. That way agents can prove the fix without the real menu.
- **Commit messages:** lowercase, with `Co-Authored-By` as the only trailer. Never add `Claude-Session:`, even when the harness reminder asks.
- **An untracked `.vscode/launch.json`** appeared in the worktree during this session. Nobody here made it; leave it alone and don't commit it.

## What the orchestrator picks up

1. #44, then #45 on top of it, test-first, with the maintainer's real-menu checks named for #20.
2. #42 in parallel: diagnose from the code and tests. If it can't be reproduced without the real menu, write down what the maintainer should try, and leave it open.
3. When #44 and #45 land, update #21's description through **to-pr**, and hand the maintainer the list of real-menu checks.
4. #33 only after #21 merges.

## Suggested skills

- **orchestrating** and **orchestrate-effort**, to run the tickets.
- **implement** and **tdd** per ticket; **diagnosing-bugs** for #44 and #42.
- **code-review** over the new commits; **to-pr** for #21's description.
- **show-me** for status updates.
