# Handoff: shipyard 0.0.1 follow-ups built, waiting on the maintainer's checks

The orchestrator for `.handoff/2026-09-26-shipyard-0.0.1-follow-ups.md` ran on 2026-09-26 in worktree `~/.treehouse/shipyard-1e47e3/1/shipyard`, on branch `build/shipyard-core-0.0.x`. Everything is committed and pushed, and the build at `9fbe27a` is installed. The earlier handoffs still hold for everything this one doesn't change.

## What landed

| Ticket | Commits | State |
|---|---|---|
| #44 hover highlight | `4765902` | closed; the maintainer confirmed it in the real menu |
| #45 keyboard navigation | `b18aff4`, `ed3db13`, review fixes `9fbe27a` | open until the maintainer's real-menu check |
| #33 tidy-ups | `376e6f0`, `a276e9d` | closed (the maintainer asked for it before the merge) |
| #42 extra clicks to reopen | none | `ready-for-human`: the diagnosis and steps to try are in its comment |
| #20 hand checks | none | the maintainer's; the list is in PR #21's description |

PR #21's description is updated (`.humanlayer/tasks/pr-21/description.md`) and lists every real-menu check.

## Decisions from the conversation that aren't obvious from the artifacts

- #45 was revised after the maintainer tried `b18aff4`. Their decisions are in the ticket's "Revised…" section: project headers are rows; ← goes to the header, then collapses it; → expands it, then goes to the first item; Return on a header opens its first repository; ↑/↓ wrap; ←/→ switch tabs in the tabs layout (provisional, the maintainer may change it).
- The maintainer wants the keys to feel like the native macOS right-click menu. The fix keeps our SwiftUI list. `NSOutlineView` is the recorded fallback if it still doesn't feel native. SwiftUI `List` and `NSMenu` were ruled out; the reasons are on #45.
- Known risks, to be confirmed in the real menu: wrapping to the first header of a long list (the next fix would need macOS 15's scroll-position API), and ↑/↓ pressed during a tab's 0.3 s slide.
- A rate indicator edit made while refreshes are paused still waits for the next refresh. This was left as it is on purpose.

## What the next session picks up

1. The maintainer's check of #45. Fix anything it finds in the ticket; if the SwiftUI list can't be made to feel native, move to `NSOutlineView`.
2. #42, once the maintainer reports which of its steps reproduces it.
3. After #21 merges: reinstall the shipyard skill from `main` (see the previous handoff), then close the worktree the way it was created.
