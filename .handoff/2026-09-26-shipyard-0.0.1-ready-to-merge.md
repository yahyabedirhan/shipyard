# Handoff: shipyard 0.0.1 is ready to merge

This session orchestrated effort `shipyard-0-0-1` on 2026-09-26: Claude Code in a Herdr tab of workspace `wJ`, worktree `~/.treehouse/shipyard-1e47e3/1/shipyard`, branch `build/shipyard-core-0.0.x`. It started from `.handoff/2026-09-26-shipyard-0.0.1-follow-ups.md`. Everything it made is committed and pushed, and the installed app is `7221483`, which differs from the branch head only in docs and comments. The earlier handoffs hold where this one doesn't contradict them.

## Where things stand

- **PR [#21](https://github.com/yahyabedirhan/shipyard/pull/21)** is open, mergeable, and not a draft. Its description is current, saved at `.humanlayer/tasks/pr-21/description.md`. The maintainer has done the QA; nothing more is needed from them before the merge.
- **Effort tickets:** every ticket labelled `effort:shipyard-0-0-1` is closed except the **spec, #1**. The latest ones, #44–#52, are summarised in the PR description. #42 and #20 were closed without further hand checks, at the maintainer's decision; the checks never walked are listed on #20.
- **Final review:** done over this round's commits, with no bugs found. Four minor points were left as they are, recorded in the review's findings: an unreachable `.ignored` case in `RowKeys.side`; a two-row list treating every step as a wrap; `ConfigStore.setLayout` truncating before it writes; and a refused layout switch not being written to `config-status.json`. None needs action unless the maintainer asks.

## What the next agent does

1. **Merge #21** when the maintainer says so. Commits `b18aff4` and `a276e9d` say "closes #45" and "closes #33"; both are already closed, so the merge changes nothing there.
2. **Update the spec, #1:** record what 0.0.1 delivered beyond it: keyboard navigation, the layout button, the account header, the starter configuration, the verdict file, and the refresh spinner. Then close it with the merge.
3. **After the merge:**
   - Reinstall the shipyard skill from `main`: `npx skills add yahyabedirhan/shipyard -s shipyard -g -a codex -y`.
   - Close the worktree the way it was created (the **init-effort-with-herdr** steps).
   - Remove the sub-agent worktrees under `~/Developer/yahyabedirhan/shipyard/.claude/worktrees/agent-*`. Everything in them is already on the branch.
4. **Leftovers from this session:**
   - The scratchpad folder `appharness` holds a GUI test harness a sub-agent built against the rules. Deleting it was blocked by a permission check; ask the maintainer to delete it or allow the deletion. It isn't running.
   - [yahyabedirhan/skills#20](https://github.com/yahyabedirhan/skills/issues/20), the QA-handoff workflow, is for the maintainer to design later. Don't act on it unasked.
   - The effort `sign-in-without-gh` (spec #22) is next in line; it wasn't touched here.
   - `.vscode/` is untracked and not ours. Leave it alone.

## Decisions from this session not obvious from the artifacts

- **QA:** only visual, interactive features get a blocking `ready-for-qa` ticket, assigned to the maintainer with how to use and try it. Configuration, skill and agent changes get none. Feedback comes in chat. The labels are in `docs/agents/triage-labels.md`.
- **Agents never run anything on the Mac:** that includes a self-built GUI harness or window, or sending key events. Verify only with `make test` and `swift build`. A sub-agent broke this rule once. Around the same time the maintainer's open apps quit and relaunched, and no cause was found.
- **The shipyard skill is for its users only:** repository and maintainer rules go in `AGENTS.md`. Code, comments and test names carry no issue numbers.
- **Label colours:** every `effort:*` label is pale brick `f9d0c4` (`docs/agents/issue-tracker.md`). `ready-for-qa` is orange.
- **Native over custom:** the maintainer rejected hand-rolled icon rotation. The refresh button shows the native `ProgressView` while refreshing.

## Suggested skills

- **to-pr** if #21's description changes again.
- **herdr** to report status or close tabs; **init-effort-with-herdr** for closing the worktree.
- **maintain-skills** for reinstalling the shipyard skill from `main`.
- **show-me** for status views the maintainer asks for.
